
class Box {
  private:
  public:
  int rowCorner, colCorner, rows, cols;
  Box(int rowCorner, int colCorner, int rows, int cols): rowCorner(rowCorner), colCorner(colCorner), rows(rows), cols(cols) {
  }
};